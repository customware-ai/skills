interface:
  display_name: "Source HTML to App UI V2"
  short_description: "Strict paired-Playwright UI reproduction workflow"
  default_prompt: "Use $source-html-to-app-ui to rebuild the approved HTML as a real app. Keep the source immutable, record its runtime defects without blocking on them, and follow every phase, evidence gate, and paired visual comparison."
