interface:
  display_name: "Source HTML to App UI"
  short_description: "Strict paired-Playwright UI reproduction workflow"
  default_prompt: "Use $source-html-to-app-ui to rebuild the provided source HTML app. Follow every phase, evidence gate, paired source-target Playwright comparison, repair loop, and completion lock exactly."
