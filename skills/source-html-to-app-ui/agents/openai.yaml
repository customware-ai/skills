interface:
  display_name: "Source HTML to App UI"
  short_description: "Rebuild source HTML apps with visual QA"
  default_prompt: "Use $source-html-to-app-ui to reproduce this source HTML app in the current target repo from its design-system JSON."
