# Migration Open Gaps

This file is the signoff-blocking migration gap ledger.
List every remaining source-vs-current mismatch or runtime gap here.
Every remaining user-visible UI or UX difference belongs here, not just missing routes or broken workflows.
Delete or mark resolved items only after the fix is verified in a new run, screenshot, or command result.
Do not mark this file empty on the first real grading pass.
Do not replace this ledger with a prose summary.
If a route exists but still uses generic filler, keep that row open.
If structure is correct but styling, product framing, copy, controls, or workflow behavior still drifts, keep that row open.
If a page still uses summary cards, placeholder prose, or a review/status shell instead of the source screen structure, keep that row open.
Keep this exact table structure.
When no unresolved ordinary gaps remain, keep the table and use:
`| None | n/a | n/a | No unresolved ordinary migration gaps remain after pass <n>. | n/a | n/a | yes |`

| Route or screen | Source evidence | Current evidence | Visible drift or runtime gap | Severity (`blocking` or `non-blocking`) | Next fix | Resolved? |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
