interface:
  display_name: "Task Workflow Skill"
  short_description: "Repeatable gated workflow for task execution with managed Playwright lifecycle"
  default_prompt: "Use $task-workflow to execute the task through artifact-backed phases, gates, managed Playwright verification, minimal warranted regression coverage, and final signoff."
