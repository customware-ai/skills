interface:
  display_name: "Task Workflow"
  short_description: "Repeatable gated workflow for task execution"
  default_prompt: "Use $task-workflow to execute the task through artifact-backed phases, gates, Playwright verification, E2E coverage, and final signoff."
